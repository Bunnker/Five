# Five 开发代理规则

本文件是 Codex、Claude Code 和其他开发代理进入 Five 仓库后的第一入口。它只保存稳定的开发约束和阅读顺序；完整需求、取舍原因和历史记录仍分别保存在 Issue、ADR、领域词汇表和评审文档中。

## 1. 开始工作前

按以下顺序读取，并以较新的明确决定为准：

1. `docs/product/prd.md`（当前为 V2.3）；
2. `docs/api/openapi.yaml` 和 `docs/api/README.md`；
3. [P0 匿名网页验证版技术启动规格（Issue #1）](https://github.com/Bunnker/Five/issues/1)；
4. 与任务相关的 `docs/architecture/adr/`；
5. `docs/domain/glossary.md`；
6. `docs/design/p0-design-brief.md`；
7. `docs/reviews/2026-07-26-p0-technical-startup-review.md`。

V2.3 已把“双端首发、多人审核”等旧要求收束为“公开网页、单人维护、大师外部确认”。V2.2 及更早版本只用于历史追溯，不得作为当前开发基线。

文档冲突时：

- 当前 PRD 是产品范围和验收的事实源；
- 较新的 `Accepted` ADR 解释稳定技术决定；若与当前 PRD 冲突，先修正文档再编码；
- Issue #1 是 P0 网页验证版的综合开发规格和任务入口；
- 领域概念和代码命名遵守领域词汇表；
- 无法由现有记录判断时，停止扩大实现范围，在 GitHub Issue 中提出明确问题。

`ready-for-agent` 表示规格已经可以按顺序执行，不表示所有公开上线条件已经完成。P0 接口已冻结在 `docs/api/openapi.yaml`，公开网页、日期模块和工程骨架可以按 Issue 顺序推进；开始后台写功能前，还要确认单人后台登录方案。

## 2. P0 产品边界

P0 只做：

- 一个匿名访问、适配手机的公开网页；
- 一个供单一维护者使用的内容后台；
- 由维护者或大师人工把网页链接发到微信群；
- 公共五行颜色、穿搭建议、已审核图片和分享落地内容。

P0 不做：

- 微信小程序和原生 App；
- 用户注册、个人档案、出生信息和个人八字；
- 大师登录后台、在线咨询或渠道管理系统；
- 微信群自动发消息；
- 吉祥物、商品、订单、支付和卖货；
- 产品内实时生图或在公开服务器部署 Codex；
- Redis、Kafka、微服务和独立消息队列；
- 为未来 App 提前搭建跨端页面框架。

发现实现开始进入上述非 P0 范围时，立即停止并回到 Issue 确认，不要以“以后可能需要”为理由预先建设。

## 3. 技术基线

### 网页

- 使用 Next.js、React 和 TypeScript；
- 优先保证微信内置浏览器的手机体验，同时覆盖 iPhone Safari 和 Android 主流浏览器；
- 网页只展示服务端返回的日期、时辰、内容版本和图片，不在浏览器中重新计算业务日期；
- 客户端类型从冻结后的 OpenAPI 契约生成，不直接导入后端内部实体；
- 复用全局视觉变量和 `visual-foundation` 基础组件，不在业务页面复制卡片、编号色带、颜色圆点和主按钮样式；
- 每个颜色圆点同时显示中文色名，白色、乳白等浅色使用可见边框；
- 先在 375px 宽度验收，页面不得横向滚动，主要点击区域不小于 44×44px；
- 不得从探索原型带入底部固定导航、历史、出生信息、个人五行、账户或其他非 P0 入口。

### 后端

- 使用 Node.js、TypeScript、NestJS 和 Fastify；
- 采用模块化单体，不按公开网页、后台或渠道拆成多个微服务；
- PostgreSQL 是 P0 唯一的业务事实数据库；
- HTTP 进程与后台 Worker 使用同一代码库和领域模块，但作为不同进程运行；
- HTTP、后台和 Worker 需要当前业务时间时统一使用 `RequestContextResolver`；只有 `SystemClock` 读取系统时钟，其他代码不得自行解释时区、命理日或时辰；
- 任务状态和触发任务的业务修改必须在同一个 PostgreSQL 事务中保存；
- Worker 必须允许安全重试，重复执行不能造成重复发布、重复海报或活跃版本漂移。

### 部署

美国服务器供应商、运行方式、PostgreSQL 托管、域名、图片存储、内容加速、备份和恢复尚未确定。不得把某个供应商的专有能力写进领域模块，也不得宣称本地通过等同于公开环境通过。

## 4. 代码架构

使用“深模块”方式组织代码：复杂规则放在模块内部，对调用方只暴露小而稳定的接口。

本仓库使用以下术语：

- **模块（Module）**：拥有一个明确接口和内部实现的代码单元；
- **接口（Interface）**：调用方必须知道的全部规则，包括输入、输出、错误、顺序和性能要求；
- **接缝（Seam）**：可以替换实现而不修改调用方的位置；
- **适配器（Adapter）**：在接缝处连接 HTTP、PostgreSQL、时钟、图片存储或外部生成工具的具体实现。

架构规则：

1. 网页页面、HTTP 控制器和 Worker 入口都是适配器，只负责接收输入、调用模块接口并返回结果；
2. 日期换算、内容状态、发布、缓存时长和图片检查等业务规则必须各自集中在唯一模块中；
3. 不得在 React 页面、HTTP 控制器、数据库查询和 Worker 中复制同一条业务规则；
4. 领域模块不得依赖 Next.js、NestJS、Fastify、数据库实体或具体云厂商；
5. 外部依赖通过接口传入，不要在领域模块内部临时创建真实数据库、系统时钟或外部客户端；
6. 模块优先返回明确结果，由外层适配器执行写库、发响应、写日志等副作用；
7. 只有确实存在两个实现或明确需要替换时才增加接缝，避免为假想未来建立大量空接口；
8. 一个模块被删除后，如果复杂规则会散落到多个调用方，说明模块有价值；如果只是少一层转发，应合并该浅模块。

建议的业务模块边界是：

- 请求上下文与命理日计算；
- 固定日历答案核对；
- 内容草稿、内容版本和生命周期；
- 排期、发布、下线和恢复；
- 每日图片组、权利材料和 AI 标识；
- 分享海报派生；
- 审计、幂等和持久化任务。

这些名称表示职责，不预先规定目录结构。首次搭建工程时，根据实际框架选择最简单的目录，不为尚不存在的第二种部署方式提前拆包。

## 5. 不可破坏的领域规则

### 日期与 `/today`

- 所有业务时间固定按 `Asia/Shanghai` 解释；
- `fortuneDate` 在北京时间 23:00 进入下一命理日；
- 跨过民用午夜时不能再次把 `fortuneDate` 多加一天；
- `requestContext` 的日期、命理日、时辰和跨日状态必须在同一时刻一次生成；
- `responseGeneratedAt` 表示这份响应内容的生成时间，不是用户收到响应时的实时时钟；
- 网页只消费服务端返回的 `requestContext`，不能用设备时间推进业务日期。

`/today` 的共享缓存秒数必须是以下四项的最小值：

- 60 秒；
- 距内容 `effectiveTo` 的剩余秒数；
- 距下一个时辰边界的剩余秒数；
- 距下一个民用午夜的剩余秒数。

使用 `Cache-Control: public, max-age=0, s-maxage=N, must-revalidate` 的语义。不得让过期内容跨日期、时辰或内容有效期继续复用。发布、下线和恢复需要主动清理相关缓存；`X-Request-Id` 必须贯穿边缘、后端日志和响应。

### 内容版本与状态

- 草稿提交核对时生成不可直接修改的内容版本和 `contentVersion`；
- 已提交、已核对或已上线内容需要修改时，复制为新草稿，不能覆盖旧版本；
- 一个 `fortuneDate` 同一时刻只能有一个 `ActiveVersion`；
- 所有关键操作保存操作者、时间、原因和操作前后的版本；
- 大师不登录后台，维护者保存大师称呼、核对时间、结论、备注和凭证引用；
- 没有大师核对凭证，正式内容不能上线。

状态只允许使用：

- 草稿：`draft`；
- 待大师核对：`in_review`；
- 需要修改：`changes_requested`；
- 可以发布：`approved`；
- 已安排上线：`scheduled`；
- 已上线：`published`；
- 已被新版本替换：`superseded`；
- 已下线：`withdrawn`。

状态转换规则只能有一份实现。已下线版本不能直接重新上线；恢复只能选择同一命理日曾经发布且当前仍安全的旧版本。

### 日历标准答案

- 固定标准答案覆盖 2026-01-01 至 2027-01-01，含首尾共 366 个日期；
- 另行覆盖 22:59、23:00、23:59、00:00、子时和有效期边界；
- 日历算法、依赖或时区处理变化后，必须重新比较全部答案；
- 任何差异都阻止发布，不能为了让测试通过而直接修改标准答案；
- 公开试用前需要大师书面复核不少于连续 30 日和特殊时间样例。

### 用户看到的档位

- 优先展示“大吉、次吉、平”；
- 底层保留完整五档结果，公开页面把“较差”和“不利”合并显示为“注意”；
- 只有“大吉、次吉、平”提供具体穿法入口；
- 用户已经穿了注意色时，可以建议使用当日大吉色普通配饰做小面积补充；
- 不使用“保证转运”等承诺性表达。

### 图片

- 图片在上线前生成、人工检查并上传，用户访问时不触发生图；
- Codex 和 GPT Image 2 是主管线，Redar 已验证的中转是可替换的备用通道；
- Five 使用独立的中转密钥和费用上限，不复用 Redar 密钥；
- 每个 `DailyImageSet` 正常包含 2 张必备图和最多 1 张可选图；
- 必备主图以大吉色为主，必备备选图使用大吉色与次吉色，可选图使用大吉色与平色或另一场景；
- 每张图片保存所属日期、内容版本、文件校验值、生成方式、声明模型、提示词版本、检查记录、权利材料和 AI 标识状态；
- 问题图片可以单独下线并换成已审核的配色卡片，不能让正确的日期和文字内容一起不可访问；
- 自动测试不得调用付费生图接口。

## 6. 数据、并发与接口规约

- PostgreSQL 迁移必须有版本、可在空库和受支持的旧版本上按顺序执行，并纳入备份与恢复验证；
- 不允许通过手工改数据库绕过内容状态和核对门槛；
- 排期、发布、下线、恢复、任务创建和任务失效必须保持事务一致；
- 使用 `lifecycleRevision`、`scheduleSlotRevision`、`If-Match` 和幂等键处理并发，不能只依赖数据库隔离；
- `docs/api/openapi.yaml` 是网页、后台和后端接口的唯一事实源；
- 状态、档位、图片字段和错误码从契约生成 TypeScript 类型，不在前后端手写两套枚举；
- 修改接口时先更新 OpenAPI、示例和契约检查，再调整调用方与实现；
- P0 只有网页端，不得重新加入 `platform`、`sourcePlatform` 或 `targetPlatform`；需要区分分发来源时只使用 `channelId`；
- 公共响应只返回公开需要的数据，不能泄露草稿、核对凭证、内部备注、密钥或权利材料原件；
- 外部错误转换为稳定的产品错误，日志保留 `X-Request-Id`，不得记录密钥或完整敏感凭证。

## 7. 编码规约

- TypeScript 开启严格类型检查，不用 `any` 绕过领域模型；
- 代码标识符使用领域词汇表中的英文名，用户文案使用清楚的中文；
- 状态名、时区、换日时间、档位和缓存上限不得散落为魔法字符串或数字；
- 业务判断使用明确的领域类型和结果，不依赖异常来表达正常分支；
- 新增依赖前说明它解决的当前问题；已有能力足够时不增加同类依赖；
- 不提交密钥、令牌、付费接口凭证、真实聊天截图或含个人信息的核对材料；
- 不读取、展示或复用 Redar 的密钥；
- 注释解释“为什么有这条规则”，不要重复代码表面行为；
- 不为未来小程序、App、多运营人员或微服务预留没有当前调用方的抽象。

工程工具已经在 Issue #4 冻结，只保留以下一套：

- Node.js 24.14.1，pnpm 10.33.0，版本分别以 `.nvmrc` 和根 `package.json` 为准；
- pnpm workspace 管理 `apps/web`、`apps/backend` 和 `packages/api-contract`，不引入额外的单仓库编排框架；
- Prettier 负责格式化，ESLint 负责代码规则，TypeScript `strict` 负责类型检查；
- Vitest 负责基础测试，网页测试使用 React Testing Library；
- PostgreSQL 17 由本地 Docker Compose 启动，迁移使用 `node-pg-migrate`，迁移文件必须提交；
- `pnpm dev` 启动本地数据库、迁移、网页、HTTP 和 Worker；
- `pnpm check` 统一检查接口契约、格式、代码规则、严格类型和基础测试；
- `pnpm smoke` 完整构建并真实验证网页、HTTP、Worker 和 PostgreSQL 可以一起工作。

`/health/live` 和 `/health/ready` 只用于判断运行状态，不是产品业务接口，因此不加入产品 OpenAPI。禁止在健康检查中返回密钥、数据库地址或其他敏感配置。

## 8. 测试规约

测试验证外部行为，不绑定框架内部结构。优先使用三个最高层接缝：

1. **发布到公开访问**：维护者发布一份通过检查的内容，匿名用户通过 `/today` 和网页看到正确的请求上下文、活跃版本、档位、图片和缓存响应；
2. **日历发布门槛**：一次比较全部 366 日标准答案和特殊时间样例；
3. **内容生命周期**：通过后台可观察行为覆盖八个状态、允许和禁止的转换、单一活跃版本、下线与安全恢复。

最低测试范围：

- 22:59→23:00、23:59→00:00、相邻时辰和 `effectiveTo`；
- `/today` 动态缓存时长、禁止跨边界复用、主动清理和请求编号；
- 没有大师凭证时禁止发布；
- 核对中的内容不能原地修改；
- 同一命理日不能出现两个活跃版本；
- Worker 重试不能重复发布或重复生成海报；
- 两张必备图、第三张可选、AI 标识、单图下线和降级卡片；
- 微信内置浏览器、iPhone Safari、Android 主流浏览器；
- 最终美国部署在不同中国大陆网络中的真实访问。

本地测试通过不等于公开试用通过。60 秒内容更新目标必须在最终内容加速服务上真实测量。

## 9. 文档与变更规约

- 新需求先进入 GitHub Issue，具体操作见 `docs/agents/issue-tracker.md`；
- Triage 标签映射见 `docs/agents/triage-labels.md`；
- 本仓库使用单一领域上下文，领域文档规则见 `docs/agents/domain.md`；
- 新的稳定领域词汇同步到 `docs/domain/glossary.md`；
- 难以回退、存在真实取舍且未来读者容易疑惑的决定使用新 ADR，不原地改写旧 ADR 的核心结论；
- 产品范围或验收变化同步更新 Issue 规格、相关 ADR、评审和 `docs/CHANGELOG.md`；
- 原型图片只作为设计参考，不是开发事实源；
- 改动文档后至少运行格式差异检查并检查相对链接；
- 不要把只存在于聊天里的决定直接实现到代码中。

## 10. 当前尚未解除的阻塞

正式功能开发前：

- 确认单人后台的登录、二次确认、账号找回和紧急停止方式。

公开试用前：

- 确认美国部署、正式域名、PostgreSQL、图片存储、内容加速、备份和恢复；
- 证明发布、下线和恢复后 60 秒内的新访问能看到正确版本；
- 生成日历标准答案并完成大师核对凭证；
- 准备首月图片、权利材料、AI 标识和下线替换流程；
- 完成用户协议、隐私说明、传统文化参考声明和反馈入口；
- 在中国大陆网络和微信内置浏览器中完成真实访问测试。
